<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fast Memo A/B 테스트 대시보드</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 40px 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        h1 {
            color: white;
            font-size: 36px;
            margin-bottom: 40px;
            text-align: center;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }

        .stat-card {
            background: white;
            border-radius: 12px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            text-align: center;
        }

        .stat-card h3 {
            font-size: 14px;
            color: #86868b;
            margin-bottom: 10px;
            text-transform: uppercase;
            font-weight: 600;
        }

        .stat-card .value {
            font-size: 36px;
            font-weight: 700;
            color: #1d1d1f;
        }

        .variant-section {
            background: white;
            border-radius: 12px;
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }

        .variant-section h2 {
            font-size: 24px;
            color: #1d1d1f;
            margin-bottom: 20px;
            border-bottom: 2px solid #f5f5f7;
            padding-bottom: 15px;
        }

        .variant-stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 20px;
            margin-bottom: 20px;
        }

        .variant-stat {
            background: #f5f5f7;
            padding: 20px;
            border-radius: 8px;
            text-align: center;
        }

        .variant-stat label {
            font-size: 12px;
            color: #86868b;
            text-transform: uppercase;
            font-weight: 600;
            display: block;
            margin-bottom: 8px;
        }

        .variant-stat .number {
            font-size: 28px;
            font-weight: 700;
            color: #1d1d1f;
        }

        .variant-stat .unit {
            font-size: 12px;
            color: #86868b;
            margin-top: 4px;
        }

        .ctr-badge {
            display: inline-block;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 8px 16px;
            border-radius: 20px;
            font-weight: 600;
            font-size: 14px;
            margin-top: 10px;
        }

        .chart-container {
            position: relative;
            height: 300px;
            margin-top: 30px;
        }

        .refresh-button {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            padding: 12px 24px;
            border-radius: 8px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: transform 0.3s ease;
            display: block;
            margin: 30px auto 0;
        }

        .refresh-button:hover {
            transform: scale(1.05);
        }

        .no-data {
            text-align: center;
            color: #86868b;
            padding: 40px;
            font-size: 18px;
        }

        .headline-text {
            background: #f5f5f7;
            padding: 15px;
            border-radius: 8px;
            margin-top: 15px;
            font-size: 16px;
            color: #1d1d1f;
            font-weight: 500;
            border-left: 4px solid #667eea;
        }

        .comparison-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        .comparison-table th {
            background: #f5f5f7;
            padding: 12px;
            text-align: left;
            font-weight: 600;
            color: #1d1d1f;
            border-bottom: 2px solid #e0e0e0;
        }

        .comparison-table td {
            padding: 12px;
            border-bottom: 1px solid #e0e0e0;
        }

        .comparison-table tr:hover {
            background: #f9f9f9;
        }

        .winner-badge {
            background: #34c759;
            color: white;
            padding: 4px 8px;
            border-radius: 4px;
            font-size: 12px;
            font-weight: 600;
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 24px;
            }

            .stats-grid {
                grid-template-columns: 1fr;
            }

            .variant-stats {
                grid-template-columns: repeat(2, 1fr);
            }

            .comparison-table {
                font-size: 14px;
            }

            .comparison-table th,
            .comparison-table td {
                padding: 8px;
            }
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>📊 Fast Memo A/B 테스트 대시보드</h1>

        <% if (Object.keys(stats.variants || {}).length === 0) { %>
            <div class="no-data">
                <p>아직 테스트 데이터가 없습니다.</p>
                <p>랜딩 페이지를 방문하면 데이터가 수집됩니다.</p>
            </div>
        <% } else { %>

            <!-- 전체 통계 -->
            <div class="stats-grid">
                <div class="stat-card">
                    <h3>총 페이지 뷰</h3>
                    <div class="value"><%= stats.totalPageViews %></div>
                </div>
                <div class="stat-card">
                    <h3>총 CTA 클릭</h3>
                    <div class="value"><%= stats.totalClicks %></div>
                </div>
                <div class="stat-card">
                    <h3>전체 클릭률</h3>
                    <div class="value">
                        <% 
                            const overallCTR = stats.totalPageViews > 0 
                                ? ((stats.totalClicks / stats.totalPageViews) * 100).toFixed(2)
                                : 0;
                        %>
                        <%= overallCTR %>%
                    </div>
                </div>
            </div>

            <!-- 각 Variant별 상세 통계 -->
            <div class="variant-section">
                <h2>📈 Variant별 성과 비교</h2>
                
                <table class="comparison-table">
                    <thead>
                        <tr>
                            <th>Variant</th>
                            <th>헤드라인</th>
                            <th>페이지 뷰</th>
                            <th>클릭</th>
                            <th>CTR</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% 
                            let bestCTR = 0;
                            let bestVariant = '';
                            Object.keys(stats.variants).forEach(key => {
                                const variant = stats.variants[key];
                                const ctr = parseFloat(variant.ctr);
                                if (ctr > bestCTR) {
                                    bestCTR = ctr;
                                    bestVariant = key;
                                }
                            });
                        %>
                        <% Object.keys(stats.variants).forEach(key => { %>
                            <% const variant = stats.variants[key]; %>
                            <tr>
                                <td><strong><%= variant.name %></strong></td>
                                <td><%= headlines[key].headline %></td>
                                <td><%= variant.pageViews %></td>
                                <td><%= variant.clicks %></td>
                                <td>
                                    <strong><%= variant.ctr %>%</strong>
                                    <% if (key === bestVariant && bestCTR > 0) { %>
                                        <span class="winner-badge">🏆 최고 성과</span>
                                    <% } %>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>

                <div class="chart-container">
                    <canvas id="ctrChart"></canvas>
                </div>
            </div>

            <!-- 각 Variant 상세 정보 -->
            <% Object.keys(stats.variants).forEach(key => { %>
                <% const variant = stats.variants[key]; %>
                <div class="variant-section">
                    <h2><%= variant.name %></h2>
                    
                    <div class="headline-text">
                        "<%= headlines[key].headline %>"
                    </div>

                    <div class="variant-stats">
                        <div class="variant-stat">
                            <label>페이지 뷰</label>
                            <div class="number"><%= variant.pageViews %></div>
                        </div>
                        <div class="variant-stat">
                            <label>CTA 클릭</label>
                            <div class="number"><%= variant.clicks %></div>
                        </div>
                        <div class="variant-stat">
                            <label>클릭률</label>
                            <div class="number"><%= variant.ctr %><span class="unit">%</span></div>
                        </div>
                    </div>

                    <% if (Object.keys(variant.scrollDepths).length > 0) { %>
                        <h3 style="margin-top: 20px; margin-bottom: 15px; font-size: 16px;">스크롤 깊이 분석</h3>
                        <div class="variant-stats">
                            <% Object.keys(variant.scrollDepths).sort((a, b) => parseInt(a) - parseInt(b)).forEach(depth => { %>
                                <div class="variant-stat">
                                    <label><%= depth %>% 깊이</label>
                                    <div class="number"><%= variant.scrollDepths[depth] %></div>
                                </div>
                            <% }); %>
                        </div>
                    <% } %>
                </div>
            <% }); %>

            <button class="refresh-button" onclick="location.reload()">🔄 새로고침</button>

        <% } %>
    </div>

    <script>
        // CTR 비교 차트
        <% if (Object.keys(stats.variants || {}).length > 0) { %>
            const ctx = document.getElementById('ctrChart');
            if (ctx) {
                const labels = [
                    <% Object.keys(stats.variants).forEach(key => { %>
                        '<%= stats.variants[key].name %>',
                    <% }); %>
                ];
                const data = [
                    <% Object.keys(stats.variants).forEach(key => { %>
                        <%= stats.variants[key].ctr %>,
                    <% }); %>
                ];

                new Chart(ctx, {
                    type: 'bar',
                    data: {
                        labels: labels,
                        datasets: [{
                            label: 'CTR (%)',
                            data: data,
                            backgroundColor: [
                                'rgba(102, 126, 234, 0.8)',
                                'rgba(118, 75, 162, 0.8)',
                                'rgba(0, 113, 227, 0.8)',
                                'rgba(52, 199, 89, 0.8)'
                            ],
                            borderColor: [
                                'rgba(102, 126, 234, 1)',
                                'rgba(118, 75, 162, 1)',
                                'rgba(0, 113, 227, 1)',
                                'rgba(52, 199, 89, 1)'
                            ],
                            borderWidth: 2,
                            borderRadius: 8
                        }]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        plugins: {
                            legend: {
                                display: false
                            }
                        },
                        scales: {
                            y: {
                                beginAtZero: true,
                                max: 100,
                                ticks: {
                                    callback: function(value) {
                                        return value + '%';
                                    }
                                }
                            }
                        }
                    }
                });
            }
        <% } %>

        // 자동 새로고침 (30초마다)
        setInterval(() => {
            location.reload();
        }, 30000);
    </script>

</body>
</html>
